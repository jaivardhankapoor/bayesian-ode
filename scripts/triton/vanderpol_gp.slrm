#!/bin/bash
#SBATCH --time=0-01:00:00    # 5 mins
#SBATCH --mem-per-cpu=1000    # 500MB of memory
#SBATCH --cpus-per-task=2
#SBATCH --array=0-64

module load anaconda3
srun ipython -- scripts/vanderpol/gp.py --json-dir 'scripts/vanderpol/json/' --id $SLURM_ARRAY_TASK_ID